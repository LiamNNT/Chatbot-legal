"""
Prompt templates for Smart Planner Agent.

The main system prompt is loaded from YAML config at runtime
(agents_config_optimized.yaml → agents.smart_planner.system_prompt).

This file can hold helper prompt fragments or overrides if needed.
"""

# Placeholder — add prompt helpers here as needed.
# Example:
# COMPLEXITY_GUIDE = "Score 0-3: simple greeting/chitchat, ..."
