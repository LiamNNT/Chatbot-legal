APP_ENV=dev
PORT=8000

# Vector store backend
VECTOR_BACKEND=faiss         # hoặc chroma
STORAGE_DIR=./storage        # nơi lưu FAISS index + docstore

# Embeddings & Reranker
EMB_MODEL=intfloat/multilingual-e5-base       # tốt cho tiếng Việt
RERANK_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2   # tuỳ chọn (có thể để trống)

# Chroma (nếu dùng)
CHROMA_PERSIST_DIR=./storage/chroma

# Logging
LOG_LEVEL=INFO