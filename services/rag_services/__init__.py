"""
rag_services — RAG (Retrieval-Augmented Generation) micro-service.

This __init__.py exists so that other services can import
``rag_services`` as a proper Python package when ``services/``
is on sys.path (eliminating the need for sys.path hacks).
"""
