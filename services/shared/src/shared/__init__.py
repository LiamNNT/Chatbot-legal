"""
Shared package — single source of truth for domain models, value objects,
exceptions, and port interfaces used across all Chatbot-UIT services.

Install in each service with:
    pip install -e ../shared
"""
